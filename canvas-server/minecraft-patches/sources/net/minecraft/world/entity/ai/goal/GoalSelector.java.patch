--- a/net/minecraft/world/entity/ai/goal/GoalSelector.java
+++ b/net/minecraft/world/entity/ai/goal/GoalSelector.java
@@ -41,8 +_,13 @@
 
     // Paper start - EAR 2
     public boolean inactiveTick() {
+        return this.inactiveTick(3); // Canvas - DAB overload (vanilla default rate)
+    }
+
+    // Canvas - DAB: tick running goals every `rate` ticks; rate is distance-scaled by the caller (Mob.inactiveTick)
+    public boolean inactiveTick(final int rate) {
         this.curRate++;
-        return this.curRate % 3 == 0; // TODO newGoalRate was already unused in 1.20.4, check if this is correct
+        return this.curRate % (rate < 1 ? 1 : rate) == 0;
     }
 
     public boolean hasTasks() {
