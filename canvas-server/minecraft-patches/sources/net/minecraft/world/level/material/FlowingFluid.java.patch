--- a/net/minecraft/world/level/material/FlowingFluid.java
+++ b/net/minecraft/world/level/material/FlowingFluid.java
@@ -60,6 +_,15 @@
     private static final int COLLISION_OCCLUSION_CACHE_SIZE = 2048;
     private static final ThreadLocal<ca.spottedleaf.moonrise.patches.collisions.util.FluidOcclusionCacheKey[]> COLLISION_OCCLUSION_CACHE = ThreadLocal.withInitial(() -> new ca.spottedleaf.moonrise.patches.collisions.util.FluidOcclusionCacheKey[COLLISION_OCCLUSION_CACHE_SIZE]);
 
+    // Canvas start - ThreadLocal allocation pool: eliminate per-fluid-tick GC pressure
+    // getNewLiquid() was allocating a new MutableBlockPos every tick per spreading block.
+    // getSpread() was allocating a new EnumMap + SpreadContext (with two fastutil maps) every call.
+    // For a 256x256 water sheet: ~65k x 3 object allocations per tick wave.
+    private static final ThreadLocal<BlockPos.MutableBlockPos> FLUID_MPOS = ThreadLocal.withInitial(BlockPos.MutableBlockPos::new);
+    private static final ThreadLocal<java.util.EnumMap<Direction, FluidState>> SPREAD_RESULT = ThreadLocal.withInitial(() -> new java.util.EnumMap<>(Direction.class));
+    private static final ThreadLocal<SpreadContext> SPREAD_CTX_POOL = ThreadLocal.withInitial(SpreadContext::new);
+    // Canvas end - ThreadLocal allocation pool
+
 
     /**
      * Due to init order, we need to use callbacks to initialise our state
@@ -162,13 +_,14 @@
                 Fluid newBelowFluidType = newBelowFluid.getType();
                 if (belowFluid.canBeReplacedWith(level, belowPos, newBelowFluidType, Direction.DOWN)
                     && canHoldSpecificFluid(level, belowPos, belowState, newBelowFluidType)) {
-                    // CraftBukkit start
-                    org.bukkit.block.Block source = org.bukkit.craftbukkit.block.CraftBlock.at(level, pos);
-                    org.bukkit.event.block.BlockFromToEvent event = new org.bukkit.event.block.BlockFromToEvent(source, org.bukkit.block.BlockFace.DOWN);
-                    level.getCraftServer().getPluginManager().callEvent(event);
-
-                    if (event.isCancelled()) {
-                        return;
+                    // CraftBukkit start - Canvas: skip event alloc when no listeners
+                    if (org.bukkit.event.block.BlockFromToEvent.getHandlerList().getRegisteredListeners().length != 0) {
+                        org.bukkit.block.Block source = org.bukkit.craftbukkit.block.CraftBlock.at(level, pos);
+                        org.bukkit.event.block.BlockFromToEvent event = new org.bukkit.event.block.BlockFromToEvent(source, org.bukkit.block.BlockFace.DOWN);
+                        level.getCraftServer().getPluginManager().callEvent(event);
+                        if (event.isCancelled()) {
+                            return;
+                        }
                     }
                     // CraftBukkit end
                     this.spreadTo(level, belowPos, belowState, Direction.DOWN, newBelowFluid);
@@ -201,13 +_,14 @@
                 BlockPos neighborPos = pos.relative(spread);
                 final BlockState neighborState = level.getBlockStateIfLoaded(neighborPos); // Paper - Prevent chunk loading from fluid flowing
                 if (neighborState == null) continue; // Paper - Prevent chunk loading from fluid flowing
-                // CraftBukkit start
-                org.bukkit.block.Block source = org.bukkit.craftbukkit.block.CraftBlock.at(level, pos);
-                org.bukkit.event.block.BlockFromToEvent event = new org.bukkit.event.block.BlockFromToEvent(source, org.bukkit.craftbukkit.block.CraftBlock.notchToBlockFace(spread));
-                level.getCraftServer().getPluginManager().callEvent(event);
-
-                if (event.isCancelled()) {
-                    continue;
+                // CraftBukkit start - Canvas: skip event alloc when no listeners
+                if (org.bukkit.event.block.BlockFromToEvent.getHandlerList().getRegisteredListeners().length != 0) {
+                    org.bukkit.block.Block source = org.bukkit.craftbukkit.block.CraftBlock.at(level, pos);
+                    org.bukkit.event.block.BlockFromToEvent event = new org.bukkit.event.block.BlockFromToEvent(source, org.bukkit.craftbukkit.block.CraftBlock.notchToBlockFace(spread));
+                    level.getCraftServer().getPluginManager().callEvent(event);
+                    if (event.isCancelled()) {
+                        continue;
+                    }
                 }
                 // CraftBukkit end
                 this.spreadTo(level, neighborPos, neighborState, spread, newNeighborFluid); // Paper - Prevent chunk loading from fluid flowing
@@ -218,7 +_,7 @@
     protected FluidState getNewLiquid(final ServerLevel level, final BlockPos pos, final BlockState state) {
         int highestNeighbor = 0;
         int neighbourSources = 0;
-        BlockPos.MutableBlockPos mutablePos = new BlockPos.MutableBlockPos();
+        BlockPos.MutableBlockPos mutablePos = FLUID_MPOS.get(); // Canvas - reuse ThreadLocal, was: new BlockPos.MutableBlockPos()
 
         for (Direction direction : Direction.Plane.HORIZONTAL) {
             BlockPos relativePos = mutablePos.setWithOffset(pos, direction);
@@ -341,30 +_,122 @@
         final LevelReader level, final BlockPos pos, final int pass, final Direction from, final BlockState state, final FlowingFluid.SpreadContext context
     ) {
         int lowest = 1000;
+        // Leaf start - optimize fluid spread
+        it.unimi.dsi.fastutil.longs.LongSet visited = ((ServerLevel) level).getCurrentWorldData().getCanvasWorldData().slopeDistanceCacheVisited;
+        SlopeDistanceNodeDeque queue = ((ServerLevel) level).getCurrentWorldData().getCanvasWorldData().slopeDistanceCacheQueue;
+        visited.clear();
+        queue.clear();
 
         for (Direction direction : Direction.Plane.HORIZONTAL) {
-            if (direction != from) {
-                BlockPos testPos = pos.relative(direction);
-                BlockState testState = context.getBlockStateIfLoaded(testPos); // Paper - Prevent chunk loading from fluid flowing
-                if (testState == null) continue; // Paper - Prevent chunk loading from fluid flowing
-                FluidState testFluidState = testState.getFluidState();
-                if (this.canPassThrough(level, this.getFlowing(), pos, state, direction, testPos, testState, testFluidState)) {
-                    if (context.isHole(testPos)) {
-                        return pass;
-                    }
-
-                    if (pass < this.getSlopeFindDistance(level)) {
-                        int v = this.getSlopeDistance(level, testPos, pass + 1, direction.getOpposite(), testState, context);
-                        if (v < lowest) {
-                            lowest = v;
-                        }
-                    }
+            if (direction == from) continue;
+
+            BlockPos testPos = pos.relative(direction); // immutable
+            BlockState testState = context.getBlockStateIfLoaded(testPos); // Paper - Prevent chunk loading from fluid flowing
+            if (testState == null) continue; // Paper - Prevent chunk loading from fluid flowing
+
+            // Check if the fluid can actually pass through to this first neighbor before adding
+            FluidState testFluidState = testState.getFluidState();
+            if (!this.canPassThrough(level, this.getFlowing(), pos, state, direction, testPos, testState, testFluidState)) {
+                continue;
+            }
+            long visitKey = encodeSlopeNode(testPos, direction.getOpposite());
+            if (visited.add(visitKey)) {
+                queue.add(new FlowingFluid.SlopeDistanceNode(testPos, pass, direction.getOpposite(), testState));
+            }
+        }
+
+        int slopeFindDistance = this.getSlopeFindDistance(level);
+
+        // Process the queue
+        while (!queue.isEmpty()) {
+            FlowingFluid.SlopeDistanceNode current = queue.poll();
+            if (context.isHole(current.pos)) {
+                return current.pass;
+            }
+
+            if (current.pass >= slopeFindDistance) continue;
+
+            for (Direction direction : Direction.Plane.HORIZONTAL) {
+                if (direction == current.from) continue;
+
+                BlockPos nextTestPos = current.pos.relative(direction); // immutable
+                BlockState nextTestState = context.getBlockStateIfLoaded(nextTestPos); // Paper - Prevent chunk loading from fluid flowing
+                if (nextTestState == null) continue; // Paper - Prevent chunk loading from fluid flowing
+
+                FluidState nextTestFluidState = nextTestState.getFluidState();
+                if (!this.canPassThrough(level, this.getFlowing(), current.pos, current.state, direction, nextTestPos, nextTestState, nextTestFluidState)) {
+                    continue;
                 }
+
+                long visitKey = encodeSlopeNode(nextTestPos, direction.getOpposite());
+                if (visited.add(visitKey)) {
+                    queue.add(new FlowingFluid.SlopeDistanceNode(nextTestPos, current.pass + 1, direction.getOpposite(), nextTestState));
+                } // Leaf end - optimize fluid spread
             }
         }
 
         return lowest;
     }
+    // Leaf start - optimize fluid spread
+
+    private static long encodeSlopeNode(final BlockPos pos, final Direction from) {
+        return ((pos.getX() & 67108863L) << 38) | ((pos.getZ() & 67108863L) << 12) | (from.ordinal() & 0x0F);
+    }
+
+    public static class SlopeDistanceNodeDeque {
+        private SlopeDistanceNode[] array;
+        private int length;
+        private int start;
+        private int end;
+
+        public SlopeDistanceNodeDeque() {
+            array = new SlopeDistanceNode[256];
+            length = array.length;
+        }
+
+            /*
+        private int size() {
+            int apparent = end - start;
+            return apparent >= 0 ? apparent : length + apparent;
+        }
+        */
+
+        private void clear() {
+            start = 0;
+            end = 0;
+        }
+
+        private boolean isEmpty() {
+            return end == start || (end <= start && length == start - end);
+        }
+
+        private SlopeDistanceNode poll() {
+            final SlopeDistanceNode t = array[start];
+            if (++start == length) start = 0;
+            return t;
+        }
+
+        private void add(final SlopeDistanceNode node) {
+            array[end++] = node;
+            if (end == length) end = 0;
+            if (end == start) resize(length, 2 * length);
+        }
+
+        private void resize(final int size, final int newLength) {
+            final SlopeDistanceNode[] newArray = new SlopeDistanceNode[newLength];
+            if (size != 0) {
+                System.arraycopy(array, start, newArray, 0, length - start);
+                System.arraycopy(array, 0, newArray, length - start, end);
+            }
+            start = 0;
+            end = size;
+            array = newArray;
+            length = newLength;
+        }
+    }
+
+    private record SlopeDistanceNode(BlockPos pos, int pass, Direction from, BlockState state) {}
+    // Leaf end - optimize fluid spread
 
     public boolean isWaterHole(
         final BlockGetter level, final BlockPos topPos, final BlockState topState, final BlockPos bottomPos, final BlockState bottomState
@@ -383,8 +_,7 @@
         final BlockState testState,
         final FluidState testFluidState
     ) {
-        return this.canMaybePassThrough(level, sourcePos, sourceState, direction, testPos, testState, testFluidState)
-            && canHoldSpecificFluid(level, testPos, testState, fluid);
+        return canHoldSpecificFluid(level, testPos, testState, fluid) && this.canMaybePassThrough(level, sourcePos, sourceState, direction, testPos, testState, testFluidState); // Canvas - cheaper checks first
     }
 
     public boolean canMaybePassThrough(
@@ -397,7 +_,7 @@
         final FluidState testFluidState
     ) {
         return !this.isSourceBlockOfThisType(testFluidState)
-            && canHoldAnyFluid(testState)
+            && (testState.tagFlag & io.canvasmc.canvas.world.block.BlockTags.CAN_HOLD_ANY_FLUID) != 0 // Canvas - optimize fluid
             && canPassThroughWall(direction, level, sourcePos, sourceState, testPos, testState);
     }
 
@@ -423,7 +_,10 @@
 
     protected Map<Direction, FluidState> getSpread(final ServerLevel level, final BlockPos pos, final BlockState state) {
         int lowest = 1000;
-        Map<Direction, FluidState> result = Maps.newEnumMap(Direction.class);
+        // Canvas start - reuse ThreadLocal EnumMap instead of allocating new one each call
+        final java.util.EnumMap<Direction, FluidState> result = SPREAD_RESULT.get();
+        result.clear();
+        // Canvas end
         FlowingFluid.SpreadContext context = null;
 
         for (Direction direction : Direction.Plane.HORIZONTAL) {
@@ -435,7 +_,7 @@
                 FluidState newFluid = this.getNewLiquid(level, testPos, testState);
                 if (canHoldSpecificFluid(level, testPos, testState, newFluid.getType())) {
                     if (context == null) {
-                        context = new FlowingFluid.SpreadContext(level, pos);
+                        context = SPREAD_CTX_POOL.get().reset(this, level, pos); // Canvas - reuse ThreadLocal SpreadContext
                     }
 
                     int distance;
@@ -479,7 +_,7 @@
     }
 
     private static boolean canHoldFluid(final BlockGetter level, final BlockPos pos, final BlockState state, final Fluid newFluid) {
-        return canHoldAnyFluid(state) && canHoldSpecificFluid(level, pos, state, newFluid);
+        return (state.tagFlag & io.canvasmc.canvas.world.block.BlockTags.CAN_HOLD_ANY_FLUID) != 0 && canHoldSpecificFluid(level, pos, state, newFluid); // Canvas - optimize fluid
     }
 
     public static boolean canHoldSpecificFluid(final BlockGetter level, final BlockPos pos, final BlockState state, final Fluid newFluid) {
@@ -500,23 +_,27 @@
             if (newFluidState.isEmpty()) {
                 fluidState = newFluidState;
                 blockState = Blocks.AIR.defaultBlockState();
-                // CraftBukkit start
-                org.bukkit.event.block.FluidLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFluidLevelChangeEvent(level, pos, blockState);
-                if (event.isCancelled()) {
-                    return;
+                // CraftBukkit start - Canvas: skip event alloc when no listeners
+                if (org.bukkit.event.block.FluidLevelChangeEvent.getHandlerList().getRegisteredListeners().length != 0) {
+                    org.bukkit.event.block.FluidLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFluidLevelChangeEvent(level, pos, blockState);
+                    if (event.isCancelled()) {
+                        return;
+                    }
+                    blockState = ((org.bukkit.craftbukkit.block.data.CraftBlockData) event.getNewData()).getState();
                 }
-                blockState = ((org.bukkit.craftbukkit.block.data.CraftBlockData) event.getNewData()).getState();
                 // CraftBukkit end
                 level.setBlock(pos, blockState, Block.UPDATE_ALL);
             } else if (newFluidState != fluidState) {
                 fluidState = newFluidState;
                 blockState = fluidState.createLegacyBlock();
-                // CraftBukkit start
-                org.bukkit.event.block.FluidLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFluidLevelChangeEvent(level, pos, blockState);
-                if (event.isCancelled()) {
-                    return;
+                // CraftBukkit start - Canvas: skip event alloc when no listeners
+                if (org.bukkit.event.block.FluidLevelChangeEvent.getHandlerList().getRegisteredListeners().length != 0) {
+                    org.bukkit.event.block.FluidLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFluidLevelChangeEvent(level, pos, blockState);
+                    if (event.isCancelled()) {
+                        return;
+                    }
+                    blockState = ((org.bukkit.craftbukkit.block.data.CraftBlockData) event.getNewData()).getState();
                 }
-                blockState = ((org.bukkit.craftbukkit.block.data.CraftBlockData) event.getNewData()).getState();
                 // CraftBukkit end
                 level.setBlock(pos, blockState, Block.UPDATE_ALL);
                 level.scheduleTick(pos, fluidState.getType(), tickDelay);
@@ -571,16 +_,25 @@
         }
     }
 
-    protected class SpreadContext {
-        private final BlockGetter level;
-        private final BlockPos origin;
+    // Canvas start - static SpreadContext for ThreadLocal pool (was non-static inner class, causing allocation on every getSpread() call)
+    protected static class SpreadContext {
+        private FlowingFluid fluid;   // set by reset(); replaces implicit FlowingFluid.this
+        private BlockGetter level;
+        private BlockPos origin;
         private final Short2ObjectMap<BlockState> stateCache = new Short2ObjectOpenHashMap<>();
         private final Short2BooleanMap holeCache = new Short2BooleanOpenHashMap();
 
-        private SpreadContext(final BlockGetter level, final BlockPos origin) {
+        SpreadContext() {} // for ThreadLocal.withInitial
+
+        SpreadContext reset(final FlowingFluid fluid, final BlockGetter level, final BlockPos origin) {
+            this.fluid = fluid;
             this.level = level;
             this.origin = origin;
+            this.stateCache.clear();
+            this.holeCache.clear();
+            return this;
         }
+        // Canvas end
 
         public BlockState getBlockState(final BlockPos pos) {
             return this.getBlockState(pos, this.getCacheKey(pos));
@@ -614,7 +_,7 @@
                 BlockState state = this.getBlockState(pos, key);
                 BlockPos below = pos.below();
                 BlockState belowState = this.level.getBlockState(below);
-                return FlowingFluid.this.isWaterHole(this.level, pos, state, below, belowState);
+                return this.fluid.isWaterHole(this.level, pos, state, below, belowState); // Canvas - was FlowingFluid.this (required non-static inner)
             });
         }
 
