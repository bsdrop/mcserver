--- a/net/minecraft/world/level/levelgen/NoiseChunk.java
+++ b/net/minecraft/world/level/levelgen/NoiseChunk.java
@@ -153,10 +_,36 @@
         }
 
         List<NoiseChunk.BlockStateFiller> builder = new ArrayList<>();
+        // Canvas start - GPU full-chunk density (Task #6): compute finalDensity at cell corners on GPU
+        // (bit-exact verified) and feed it into MC's own interpolation via interpolated(gpuGrid).
+        // Correct-by-construction; off-grid + not-ready + GPU-busy all fall back to CPU.
+        DensityFunction densitySource = wrappedRouter.finalDensity();
+        double[] gpuGrid = io.canvasmc.canvas.gpu.worldgen.GPUSurfacePrecomputer.computeDensityGrid(
+            randomState, chunkMinBlockX, chunkMinBlockZ, this.cellWidth, this.cellHeight,
+            cellCountXZ, this.cellCountY, this.cellNoiseMinY);
+        int expectedGrid = (cellCountXZ + 1) * (cellCountXZ + 1) * (this.cellCountY + 1);
+        // GPU grid is computed from randomState.router().finalDensity(); the fill uses
+        // wrappedRouter = router.mapAll(wrap), which only DIFFERS when blending is active (BlendDensity/
+        // BlendAlpha at old-chunk boundaries). So only use the GPU grid when blender.isEmpty() (the
+        // common case incl. all fresh worlds); blend-boundary chunks fall back to CPU. This cheap check
+        // replaces the old 3-corner finalDensity gate (which cost 3 full tree-walks/chunk on CPU).
+        if (gpuGrid != null && gpuGrid.length == expectedGrid && this.blender.isEmpty()) {
+            // Value kernel was verified bit-exact (8192/8192) at context creation; cache is per-dimension
+            // with matching grid size (length guard) — grid is trusted with NO per-chunk CPU re-compute.
+            GpuGridDensity gpu = new GpuGridDensity(gpuGrid, chunkMinBlockX, chunkMinBlockZ,
+                this.cellWidth, this.cellHeight, cellCountXZ + 1, cellCountXZ + 1, this.cellCountY + 1,
+                this.cellNoiseMinY, densitySource);
+            densitySource = DensityFunctions.interpolated(gpu);
+            io.canvasmc.canvas.gpu.worldgen.GPUSurfacePrecomputer.reportDensityUsed(gpuGrid.length);
+        } else {
+            // Canvas - GPU density miss (CPU fallback): gpuGrid null (tryLock fail/off-grid), size mismatch, or blend boundary
+            io.canvasmc.canvas.gpu.worldgen.GPUSurfacePrecomputer.reportDensityMiss();
+        }
         DensityFunction fullNoiseValue = DensityFunctions.cacheAllInCell(
-                DensityFunctions.add(wrappedRouter.finalDensity(), DensityFunctions.BeardifierMarker.INSTANCE)
+                DensityFunctions.add(densitySource, DensityFunctions.BeardifierMarker.INSTANCE)
             )
             .mapAll(this::wrap);
+        // Canvas end - GPU full-chunk density
         this.fullNoiseDensity = fullNoiseValue;
         builder.add(context -> this.aquifer.computeSubstance(context, fullNoiseValue.compute(context)));
         if (settings.oreVeinsEnabled()) {
@@ -228,6 +_,11 @@
         return Mth.floor(this.preliminarySurfaceLevel.compute(new DensityFunction.SinglePointContext(blockX, 0, blockZ)));
     }
 
+    /** Canvas GPU: pre-populate surface height cache from GPU precomputation results. */
+    public void canvas$prePopulateSurfaceCache(it.unimi.dsi.fastutil.longs.Long2IntOpenHashMap entries) {
+        this.preliminarySurfaceLevelCache.putAll(entries);
+    }
+
     private void fillSlice(final boolean slice0, final int cellX) {
         this.cellStartBlockX = cellX * this.cellWidth;
         this.inCellX = 0;
@@ -373,7 +_,17 @@
     }
 
     protected DensityFunction wrap(final DensityFunction function) {
-        return this.wrapped.computeIfAbsent(function, this::wrapNew);
+        // Canvas start - reduce worldgen allocations
+        // avoid lambda allocation
+        DensityFunction func = this.wrapped.get(function);
+
+        if (func == null) {
+            func = this.wrapNew(function);
+            this.wrapped.put(function, func);
+        }
+
+        return func;
+        // Canvas end - reduce worldgen allocations
     }
 
     private DensityFunction wrapNew(final DensityFunction function) {
